@{
    if(Roles.IsUserInRole("vanthu")){
        var db = Database.Open("HOME_PHUMY");
        int status = Convert.ToInt16(Request["status"]);
        int newStatus;
        if(status == 1){
            newStatus = 0;
        }else{
            newStatus = 1;
        }

        string updateCommand = "UPDATE Config Set LockUpdateWeekJob = " + newStatus;
        db.Execute(updateCommand);
    }
    
}