@{
    if (Roles.IsUserInRole("vanthu") || Roles.IsUserInRole("admin"))
    {
        var db = Database.Open("HOME_PHUMY");
        var updateQueryString = "UPDATE MeetingSchedule SET pheduyet=1 WHERE thoigian >= @0 AND thoigian < @1";
        db.Execute(updateQueryString, Request["fromDate"],Request["toDate"]);
        //Response.Write(updateQueryString);
    }
}